import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  menuOption: {
    paddingHorizontal: 16,
    paddingVertical: 8,
  },
  menuOptions: {
    paddingVertical: 8,
    borderRadius: 8,
  },
});
